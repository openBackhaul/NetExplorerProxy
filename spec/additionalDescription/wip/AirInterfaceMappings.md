# AirInterface Data Output Mapping

This document describes how the air interface data shall be mapped from cached data into the required output formats.  
Notice that the air interface data is split into several output data "classes", i.e. distributed across multiple services.  
These services shall provide the data gathered from all devices (if desired filtered for specific devices by a handed over list of devices).